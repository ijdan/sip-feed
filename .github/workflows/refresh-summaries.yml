name: Refresh summaries — reformulation IA des articles bruts

on:
  workflow_dispatch:
    inputs:
      date:
        description: "Jour à retraiter (YYYY-MM-DD, UTC)"
        required: true
      dry_run:
        description: "Mode dry-run (liste les articles concernés, n'écrit rien)"
        type: boolean
        required: false
        default: true
      batch_size:
        description: "Taille des lots envoyés à Gemini"
        required: false
        default: '20'

env:
  GCP_PROJECT: tech-news-aggregator-001

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: cd collector && pip install -r requirements.txt --quiet

      - uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: google-github-actions/setup-gcloud@v3

      - name: Récupère GEMINI_API_KEY depuis Secret Manager
        run: |
          KEY=$(gcloud secrets versions access latest --secret=GEMINI_API_KEY --project=${{ env.GCP_PROJECT }})
          echo "::add-mask::$KEY"
          echo "GEMINI_API_KEY=$KEY" >> "$GITHUB_ENV"

      - name: Lance la reformulation
        env:
          FIRESTORE_PROJECT_ID: ${{ env.GCP_PROJECT }}
          GRPC_DNS_RESOLVER: native
        run: |
          cd collector
          ARGS="${{ inputs.date }} --batch-size ${{ inputs.batch_size }}"
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          python refresh_summaries.py $ARGS
